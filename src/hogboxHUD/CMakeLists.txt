
IF(DYNAMIC_hogbox)
    ADD_DEFINITIONS(-DHOGBOXHUD_LIBRARY)
ELSE()
    ADD_DEFINITIONS(-DHOGBOX_LIBRARY_STATIC)
ENDIF()

SET(LIB_NAME hogboxHUD)
SET(HEADER_PATH ${hogbox_SOURCE_DIR}/include/${LIB_NAME})

SET(TARGET_H
    ${HEADER_PATH}/Export.h
    ${HEADER_PATH}/Hud.h
    ${HEADER_PATH}/HudInputEvent.h
    ${HEADER_PATH}/HudInputHandler.h
    ${HEADER_PATH}/HudEventCallback.h
    ${HEADER_PATH}/Region.h
    ${HEADER_PATH}/StrokeRegion.h
    ${HEADER_PATH}/TextRegion.h
    ${HEADER_PATH}/ButtonRegion.h
)

# FIXME: For OS X, need flag for Framework or dylib
SET(TARGET_SRC
	Hud.cpp
	HudInputHandler.cpp
	Region.cpp
    StrokeRegion.cpp
	TextRegion.cpp
    ButtonRegion.cpp
)

SET(TARGET_LIBRARIES hogbox)

SET(TARGET_EXTERNAL_LIBRARIES    
    OSG_LIBRARY
    OSGDB_LIBRARY
    OSGTEXT_LIBRARY
    OSGUTIL_LIBRARY
    OSGVIEWER_LIBRARY
	OSGGA_LIBRARY
	OSGANIMATION_LIBRARY
    OPENTHREADS_LIBRARY
)
#LINK_EXTERNAL(${LIB_NAME} ${OPENGL_LIBRARIES}) 
#LINK_CORELIB_DEFAULT(${LIB_NAME})

#SET_TARGET_PROPERTIES(${LIB_NAME} PROPERTIES DEBUG_POSTFIX "d")
#if(MSVC)
#    SET_TARGET_PROPERTIES(${LIB_NAME} PROPERTIES PREFIX "../")
#    SET_TARGET_PROPERTIES(${LIB_NAME} PROPERTIES IMPORT_PREFIX "../")
#endif(MSVC)

#INCLUDE(ModuleInstall OPTIONAL)

SETUP_LIBRARY(${LIB_NAME})